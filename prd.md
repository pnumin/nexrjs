# 제품 요구사항 정의서 (PRD): OpenDART 공시 및 기업 정보 대시보드

본 문서는 금융감독원 전자공시 OPENDART API를 활용하여 기업 공시 정보와 기업 개황을 효율적으로 조회하고 분석할 수 있는 프리미엄 웹 대시보드의 제품 요구사항을 정의합니다.

---

## 1. 프로젝트 개요
* **프로젝트명**: OpenDART Web Dashboard
* **목적**: OPENDART 오픈 API를 활용하여, 투자자 및 분석가가 실시간 공시 목록과 기업 상세 개황을 한눈에 조회하고 필터링할 수 있는 프리미엄 웹 대시보드 개발
* **API Key**: `517f6e68651d9dcb10b306a9aeaca2f466e1e941`
* **개발 프레임워크**: Next.js (App Router 기반)
* **디자인 시스템**: Vanilla CSS (CSS Modules) 기반 프리미엄 테마. 다크 모드(Dark Mode) 및 라이트 모드(Light Mode) 간 실시간 테마 토글 제공.

---

## 2. 핵심 기능 요구사항

### 2.1. 실시간 공시 검색 및 목록 조회 (공시검색 API 연동)
* **목록 조회**: 
  * 전체 혹은 특정 회사의 최근 공시 보고서 목록을 실시간으로 가져옵니다.
  * 기본 정렬: 접수일자 내림차순(최신순)
* **상세 필터**:
  * **기간 설정**: 시작일(bgn_de) ~ 종료일(end_de) 선택 가능 (회사명 없을 시 최대 3개월 제한 조건 반영)
  * **법인 구분**: 전체, 유가증권시장(Y), 코스닥(K), 코넥스(N), 기타법인(E) 라디오/드롭다운 필터
  * **공시 유형**: 정기공시(A), 주요사항보고(B), 발행공시(C), 지분공시(D) 등 체크박스 멀티 필터링
* **공시 뷰어 이동**:
  * 목록 내 보고서명 클릭 시, 금융감독원 공식 DART 공시 뷰어 PC 웹페이지(`https://dart.fss.or.kr/dsaf001/main.do?rcpNo={접수번호}`)로 새 창 연결

### 2.2. 회사 검색 및 고유번호 자동완성 (고유번호 API 연동)
* **백엔드 고유번호 캐싱 서비스**:
  * OPENDART의 고유번호 API(`https://opendart.fss.or.kr/api/corpCode.xml`)는 대형 ZIP 파일(전체 회사 매핑 데이터)을 반환합니다.
  * 서버 시작 시 또는 최초 요청 시 이 ZIP을 다운로드하여 압축을 풀고, XML을 파싱해 내부 메모리/로컬 파일에 캐싱합니다.
* **실시간 자동완성 검색**:
  * 사용자가 대시보드 상단 검색창에 회사명 또는 종목코드를 입력하면 백엔드 캐시에서 즉시 검색하여 연관 회사 목록을 드롭다운으로 제안합니다.
  * 회사 선택 시 해당 회사의 `corp_code`를 획득하여 검색 컨텍스트를 업데이트합니다.

### 2.3. 기업 개황 정보 시각화 (기업개황 API 연동)
* **기업 개황 정보 카드**:
  * 특정 회사가 선택되면 해당 회사의 `corp_code`로 기업개황 API(`https://opendart.fss.or.kr/api/company.json`)를 호출합니다.
  * 조회된 데이터를 카드 레이아웃으로 미려하게 시각화합니다.
  * 표시 정보: 회사 정식명칭(국문/영문), 대표자명, 종목코드, 법인구분, 법인등록번호, 사업자등록번호, 주소, 대표전화/팩스, 홈페이지/IR URL, 설립일, 결산월 등
* **공시 목록 연계**:
  * 특정 회사의 개황을 보면서, 해당 회사가 제출한 공시 목록만 필터링하여 조회할 수 있는 퀵 버튼을 지원합니다.

### 2.4. 보안, 성능 및 테마 편의 기능
* **API Key 보안**:
  * API Key는 서버사이드 환경 변수(`.env.local`)로 관리하며, 클라이언트 단 브라우저에 절대 직접 노출되지 않도록 Next.js Route Handlers(API Routes)를 통해 백엔드 프록시로 동작합니다.
* **다크/라이트 테마 토글**:
  * 사용자가 다크 모드와 라이트 모드를 실시간으로 선택하여 전환할 수 있는 토글 버튼을 헤더 영역에 구현합니다.
  * 기본 설정은 시스템 테마를 따르거나 다크 모드를 디폴트로 하며, 선택된 테마는 브라우저 로컬 스토리지에 저장되어 재방문 시에도 유지됩니다.
* **로딩 상태 및 스켈레톤 UI**:
  * API 지연 시 사용자가 지루하지 않도록 스켈레톤 로딩 애니메이션과 스피너를 적용합니다.

---

## 3. UI/UX 디자인 가이드

* **비주얼 컨셉**: 프리미엄 다크 & 라이트 듀얼 테마
  * **다크 모드**: Deep slate blue / Gray (#0B0F19, #1E293B) 배경, Electric Cyan / Neon Purple (#06B6D4, #8B5CF6) 강조색, Glassmorphism 반투명 효과 적용
  * **라이트 모드**: Pure White / Light Slate (#F8FAFC, #FFFFFF) 배경, Deep Navy / Indigo (#1E1B4B, #4F46E5) 강조색, 부드러운 박스 섀도우를 통한 세련된 카드 레이아웃 구현
* **레이아웃 구조**:
  * **Sidebar**: 좌측 고정. 대시보드 메인, 회사별 개황, 설정 정보 바로가기 탭
  * **Header**: 상단 고정. 회사 검색 자동완성 바 + **다크/라이트 모드 스위치 버튼** + API 상태 표시
  * **Dashboard Panel (Main)**:
    * 상단: 검색/필터 폼 및 현재 검색 조건 요약 배지
    * 중앙: 공시 목록 테이블 (반응형 그리드, Hover 시 열 하이라이팅 효과)
    * 우측 또는 하단 슬라이드패널: 선택한 기업의 개황 정보 디테일 뷰

---

## 4. 비기능 요구사항
* **웹 접근성 및 검색 엔진 최적화 (SEO)**:
  * Next.js 메타데이터 설정을 통해 대시보드 페이지 타이틀, 설명 최적화
  * 시맨틱 HTML5 태그 사용 (`<aside>`, `<header>`, `<main>`, `<section>`, `<article>`)
  * 모든 양식에 고유한 `id` 부여
* **예외 처리**:
  * DART API 호출 횟수 초과(하루 2만건 제한) 또는 유효하지 않은 API 키일 경우 명확한 에러 메시지(Alert 컴포넌트) 출력
  * 네트워크 장애 또는 데이터가 존재하지 않는 경우(예: 비상장 회사 중 개황이 아직 없는 경우 등)의 안내 화면 제공
